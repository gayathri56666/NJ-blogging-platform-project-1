<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Simple Blogging Platform</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f3f4f6;
      margin: 0;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #333;
    }

    #blogForm {
      max-width: 600px;
      margin: 20px auto;
      background: white;
      padding: 20px;
      border-radius: 10px;
      box-shadow: 0 0 5px rgba(0,0,0,0.2);
    }

    input, textarea {
      width: 100%;
      padding: 10px;
      margin-top: 10px;
      border: 1px solid #ccc;
      border-radius: 6px;
    }

    button {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 10px 15px;
      border-radius: 5px;
      cursor: pointer;
      margin-top: 10px;
    }

    button:hover {
      background-color: #0056b3;
    }

    .post {
      background: white;
      padding: 15px;
      border-radius: 10px;
      margin: 20px auto;
      max-width: 600px;
      box-shadow: 0 0 5px rgba(0,0,0,0.1);
    }

    .post h2 {
      color: #222;
    }

    .post p {
      color: #555;
    }

    .delete-btn {
      background: crimson;
      padding: 5px 10px;
      border: none;
      color: white;
      border-radius: 5px;
      cursor: pointer;
      float: right;
    }

    .delete-btn:hover {
      background: darkred;
    }
  </style>
</head>
<body>
  <h1>📝 My Simple Blog</h1>

  <form id="blogForm">
    <input type="text" id="title" placeholder="Enter blog title" required />
    <textarea id="content" rows="5" placeholder="Write your blog content..." required></textarea>
    <button type="submit">Publish Post</button>
  </form>

  <div id="posts"></div>

  <script>
    // Load posts from localStorage
    document.addEventListener("DOMContentLoaded", showPosts);
    document.getElementById("blogForm").addEventListener("submit", addPost);

    function addPost(e) {
      e.preventDefault();
      let title = document.getElementById("title").value;
      let content = document.getElementById("content").value;

      let posts = JSON.parse(localStorage.getItem("posts")) || [];
      posts.push({ title, content });
      localStorage.setItem("posts", JSON.stringify(posts));

      document.getElementById("title").value = "";
      document.getElementById("content").value = "";
      showPosts();
    }

    function showPosts() {
      let posts = JSON.parse(localStorage.getItem("posts")) || [];
      let postContainer = document.getElementById("posts");
      postContainer.innerHTML = "";

      if (posts.length === 0) {
        postContainer.innerHTML = "<p style='text-align:center;'>No posts yet! Write one above.</p>";
      } else {
        posts.forEach((post, index) => {
          postContainer.innerHTML += `
            <div class="post">
              <button class="delete-btn" onclick="deletePost(${index})">Delete</button>
              <h2>${post.title}</h2>
              <p>${post.content}</p>
            </div>
          `;
        });
      }
    }

    function deletePost(index) {
      let posts = JSON.parse(localStorage.getItem("posts")) || [];
      posts.splice(index, 1);
      localStorage.setItem("posts", JSON.stringify(posts));
      showPosts();
    }
  </script>
</body>
</html>
